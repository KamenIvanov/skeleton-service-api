buildJdk=${java.version}
buildJvm=${java.vm.vendor} ${java.vm.name} ${java.vm.version}
buildOs=${os.name} ${os.version} (${os.arch})
buildTime=${git.build.time}
builtBy=${user.name}
scmBranch=${git.branch}
scmRevision=${git.commit.id}
scmRevisionFull=${git.commit.id.full}
vendor=${project.organization.name}
vendorId=${project.groupId}
artifactId=${project.artifactId}
version=${project.version}
schemaName=${project.artifactId}
